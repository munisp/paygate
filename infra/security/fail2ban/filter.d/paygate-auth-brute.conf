# Fail2Ban filter for APISIX 401 responses (auth brute-force)
[Definition]
failregex = .*"status":401.*"remote_addr":"<HOST>".*
            .*"remote_addr":"<HOST>".*"status":401.*
ignoreregex = .*\/api\/health.*
datepattern = %%Y-%%m-%%dT%%H:%%M:%%S
