fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.9.2
httpx==0.28.1
pillow==11.0.0
opencv-python-headless==4.10.0.84
numpy==1.26.4
boto3==1.35.0
confluent-kafka==2.15.0
# PaddleOCR (install separately with GPU support if available)
paddlepaddle==3.0.0
paddleocr==2.10.0
# Docling (IBM Research document understanding)
docling==2.5.0
# Optional: for VLM integration
# openai==1.40.0  # if using GPT-4V
